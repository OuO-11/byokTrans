// src/nativeProxy.js (Deprecated stub to fix build errors)
export async function fetchNativeDirect(url) {
  console.warn("fetchNativeDirect is deprecated. WebViewManager handles fetching natively.");
  return { html: "", url: url };
}
